# Pinned to versions verified working on the AML A100 image
# (Ubuntu 20.04 + Python 3.10 + CUDA 12.4 driver 570.x).
# Install order matters: install torch FIRST (or use the AML base image torch),
# then the rest. See setup.md for one-shot instructions.

# ---------- Core DL stack (skip if base image already has torch 2.5.x cu124) ----------
# torch==2.5.1            # +cu124 wheel from https://download.pytorch.org/whl/cu124
# torchvision             # match torch
# torchaudio              # match torch

# ---------- HuggingFace + training ----------
transformers==4.51.3
tokenizers>=0.21,<0.22
accelerate==1.13.0
deepspeed==0.15.1
bitsandbytes==0.49.2

# ---------- Numerics / data ----------
numpy>=1.26,<2.2
pandas==1.5.3
pyarrow==18.0.0
scikit-learn==1.5.2
scipy
xgboost==3.2.0

# ---------- Databricks data pull ----------
databricks-sql-connector==4.2.5
PyJWT
cryptography
requests
urllib3
charset-normalizer
idna
certifi

# ---------- CLI / logging ----------
fire==0.7.1
wandb==0.26.0

# ---------- Optional: feature analysis notebook ----------
# lightgbm
# matplotlib
# seaborn
